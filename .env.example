# Application Configuration
APP_NAME=do-template-cli
APP_VERSION=1.0.0
APP_ENVIRONMENT=development
APP_DEBUG=false

# Logger Configuration
LOGGER_LEVEL=info
LOGGER_FORMAT=console
LOGGER_OUTPUT=stdout
LOGGER_NO_COLOR=false

# Example usage:
# cp .env.example .env
# Then edit .env with your preferred values
#
# The application will automatically pick up these environment variables
# and use them to configure the CLI behavior.